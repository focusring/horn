[package]
name = "horn-gui"
version = "0.1.0"
edition = "2024"
description = "Web GUI for Horn PDF/UA accessibility checker"
license = "MIT"

[dependencies]
axum = "0.8"
tokio = { version = "1", features = ["full"] }
tower-http = { version = "0.6", features = ["fs"] }
